apiVersion: v1
kind: ServiceAccount
metadata:
  name: techchallenge-api
# A aplicação nunca chama a API do Kubernetes, então não existe Role nem
# RoleBinding — menor privilégio possível — e o token não é montado.
automountServiceAccountToken: false
