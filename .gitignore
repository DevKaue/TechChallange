node_modules/
dist/
coverage/
coverage-integration/
coverage-e2e/
combined-coverage/
.env
.env.*
!.env.example
# Necessário: o QUICKSTART manda copiar este arquivo para criar o .env.secret.
# Sem ele versionado, quem clona o repo não consegue subir o ambiente local.
!k8s/overlays/*/.env.secret.example
npm-debug.log*
.DS_Store

.mcplens/
opencode.json.idea/
.idea/
opencode.json
.opencode
.scannerwork/
.claude
# Segredos reais dos overlays do Kubernetes
k8s/overlays/*/.env.secret

# Terraform — redundante com terraform/.gitignore de propósito: o repositório é
# público e o state contém DATABASE_URL, JWT_SECRET, PASSWORD_SALT e
# WEBHOOK_SECRET em texto claro.
*.tfstate
*.tfstate.*
*.tfvars
!terraform/example.tfvars
**/.terraform/